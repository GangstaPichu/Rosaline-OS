[customizations.installer.kickstart]
contents = """
# Rosaline OS default kickstart
# Keeps the installer interactive (no autopart/timezone assumptions baked
# in) while pointing it at this image as the ostree/bootc container source.
ostreecontainer --url=ghcr.io/gangstapichu/rosaline-os:latest --transport=registry
"""
